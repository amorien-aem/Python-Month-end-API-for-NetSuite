services:
  - name: python-month-end-api
    type: web
    env: docker
    branch: main
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 3000
      - key: API_KEY
        generate: true
    healthCheck:
      path: /health
      protocol: HTTP
      port: 3000
      intervalSeconds: 10
      timeoutSeconds: 3
      healthyThreshold: 2
      unhealthyThreshold: 3
